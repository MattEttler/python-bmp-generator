

def convert_rgb_to_yCbCr(pixel_array):
    """ 
    Convert an array of RGB pixels (every three tuples represents one pixel)
    into YCbCr colorspace.

    Keyword arguments:
    rgb_pixel_array -- where each value is a whole number between 0 and 255
    red -- the intensity value of red which can be set between 0 - 255
    blue -- the intensity value of blue which can be set between 0 - 255
    green -- the intensity value of green which can be set between 0 - 255
    """
